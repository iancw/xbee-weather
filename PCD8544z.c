#include "PCD8544z.h"
#include "PCD8544_spi.h"
#include "clock.h"
#include "glcdfont.h"
#include <stdlib.h>

#define OUTPUT 0
#define LOW 0
#define HIGH 1
#define swap(a, b) { unsigned char t = a; a = b; b = t; }
#define _BV( bit ) ( 1<<(bit) ) 

void my_setpixel(unsigned char x, unsigned char y, unsigned char color);
unsigned char pgm_read_byte(char * ptr);
char abs(char c)
{
	if(c < 0)
	{
		return (c*-1);
	}
		
	return c;
}

unsigned char pgm_read_byte(const unsigned char * ptr)
{
	return *ptr;
}

// the memory buffer for the LCD
unsigned char pcd8544_buffer[LCDWIDTH * LCDHEIGHT / 8] = {
, 
};

// reduces how much is refreshed, which speeds it up!
// originally derived from Steve Evans/JCW's mod but cleaned up and
// optimized
//#define enablePartialUpdate

#ifdef enablePartialUpdate
unsigned char xUpdateMin, xUpdateMax, yUpdateMin, yUpdateMax;
#endif



void updateBoundingBox(unsigned char xmin, unsigned char ymin, unsigned char xmax, unsigned char ymax) {
#ifdef enablePartialUpdate
  if (xmin < xUpdateMin) xUpdateMin = xmin;
  if (xmax > xUpdateMax) xUpdateMax = xmax;
  if (ymin < yUpdateMin) yUpdateMin = ymin;
  if (ymax > yUpdateMax) yUpdateMax = ymax;
#endif
}

void drawbitmap(unsigned char x, unsigned char y, 
			unsigned char *bitmap, unsigned char w, unsigned char h,
			unsigned char color) {
    unsigned char j, i;
  for (j=0; j<h; j++) {
    for (i=0; i<w; i++ ) {
      if (pgm_read_byte(bitmap + i + (j/8)*w) & _BV(j%8)) {
		my_setpixel(x+i, y+j, color);
      }
    }
  }

  updateBoundingBox(x, y, x+w, y+h);
}


void drawstring(unsigned char x, unsigned char line, char *c) {
  while (c[0] != 0) {
    drawchar(x, line, c[0]);
    c++;
    x += 6; // 6 pixels wide
    if (x + 6 > LCDWIDTH) {
      x = 0;    // ran out of this line
      line++;
    }
    if (line >= (LCDHEIGHT/8))
      return;        // ran out of space :(
  }
}


void drawstring_P(unsigned char x, unsigned char line, const char *str) {
  while (1) {
    char c = pgm_read_byte(str++);
    if (! c)
      return;
    drawchar(x, line, c);
    x += 6; // 6 pixels wide
    if (x + 6 > LCDWIDTH) {
      x = 0;    // ran out of this line
      line++;
    }
    if (line >= (LCDHEIGHT/8))
      return;        // ran out of space :(
  }
}

void  drawchar(unsigned char x, unsigned char line, char c) {
   unsigned char i; 
  if (line >= LCDHEIGHT/8) return;
  if ((x+5) >= LCDWIDTH) return;

  for (i =0; i<5; i++ ) {
    pcd8544_buffer[x + (line*LCDWIDTH) ] = pgm_read_byte(font+(c*5)+i);
    x++;
  }

  updateBoundingBox(x, line*8, x+5, line*8 + 8);
}

// bresenham's algorithm - thx wikpedia
void drawline(unsigned char x0, unsigned char y0, unsigned char x1, unsigned char y1, 
		      unsigned char color) {
  unsigned char dx, dy;
  unsigned char steep = abs(y1 - y0) > abs(x1 - x0);
  char err;
  char ystep;

  if (steep) {
    swap(x0, y0);
    swap(x1, y1);
  }

  if (x0 > x1) {
    swap(x0, x1);
    swap(y0, y1);
  }

  // much faster to put the test here, since we've already sorted the points
  updateBoundingBox(x0, y0, x1, y1);

  dx = x1 - x0;
  dy = abs(y1 - y0);
  err = dx / 2;

  if (y0 < y1) {
    ystep = 1;
  } else {
    ystep = -1;}

  for (; x0<=x1; x0++) {
    if (steep) {
      my_setpixel(y0, x0, color);
    } else {
      my_setpixel(x0, y0, color);
    }
    err -= dy;
    if (err < 0) {
      y0 += ystep;
      err += dx;
    }
  }
}


// filled rectangle
void fillrect(unsigned char x, unsigned char y, unsigned char w, unsigned char h, 
		      unsigned char color) {
unsigned char i, j; 
  // stupidest version - just pixels - but fast with internal buffer!
  for (i=x; i<x+w; i++) {
    for (j=y; j<y+h; j++) {
      my_setpixel(i, j, color);
    }
  }

  updateBoundingBox(x, y, x+w, y+h);
}

// draw a rectangle
void drawrect(unsigned char x, unsigned char y, unsigned char w, unsigned char h, 
		      unsigned char color) {
unsigned char i; 
  // stupidest version - just pixels - but fast with internal buffer!
  for (i=x; i<x+w; i++) {
    my_setpixel(i, y, color);
    my_setpixel(i, y+h-1, color);
  }
  for (i=y; i<y+h; i++) {
    my_setpixel(x, i, color);
    my_setpixel(x+w-1, i, color);
  } 

  updateBoundingBox(x, y, x+w, y+h);
}

// draw a circle outline
void drawcircle(unsigned char x0, unsigned char y0, unsigned char r, 
			unsigned char color) {
 char f, ddF_x, ddF_y, x, y;
	f = 1 - r;
   ddF_x = 1;
   ddF_y = -2 * r;
   x = 0;
   y = r;

  updateBoundingBox(x0-r, y0-r, x0+r, y0+r);

  my_setpixel(x0, y0+r, color);
  my_setpixel(x0, y0-r, color);
  my_setpixel(x0+r, y0, color);
  my_setpixel(x0-r, y0, color);

  while (x<y) {
    if (f >= 0) {
      y--;
      ddF_y += 2;
      f += ddF_y;
    }
    x++;
    ddF_x += 2;
    f += ddF_x;
  
    my_setpixel(x0 + x, y0 + y, color);
    my_setpixel(x0 - x, y0 + y, color);
    my_setpixel(x0 + x, y0 - y, color);
    my_setpixel(x0 - x, y0 - y, color);
    
    my_setpixel(x0 + y, y0 + x, color);
    my_setpixel(x0 - y, y0 + x, color);
    my_setpixel(x0 + y, y0 - x, color);
    my_setpixel(x0 - y, y0 - x, color);
    
  }
}

void fillcircle(unsigned char x0, unsigned char y0, unsigned char r, 
			unsigned char color) {

	char f, ddF_x, ddF_y, x, y;
	unsigned char i;

  updateBoundingBox(x0-r, y0-r, x0+r, y0+r);
  f = 1 - r;
   ddF_x = 1;
   ddF_y = -2 * r;
   x = 0;
   y = r;

  for (i=y0-r; i<=y0+r; i++) {
    my_setpixel(x0, i, color);
  }

  while (x<y) {
    if (f >= 0) {
      y--;
      ddF_y += 2;
      f += ddF_y;
    }
    x++;
    ddF_x += 2;
    f += ddF_x;
  
    for (i=y0-y; i<=y0+y; i++) {
      my_setpixel(x0+x, i, color);
      my_setpixel(x0-x, i, color);
    } 
    for (i=y0-x; i<=y0+x; i++) {
      my_setpixel(x0+y, i, color);
      my_setpixel(x0-y, i, color);
    }    
  }
}


void my_setpixel(unsigned char x, unsigned char y, unsigned char color) {
  if ((x >= LCDWIDTH) || (y >= LCDHEIGHT))
    return;

  // x is which column
  if (color) 
    pcd8544_buffer[x+ (y/8)*LCDWIDTH] |= _BV(y%8);  
  else
    pcd8544_buffer[x+ (y/8)*LCDWIDTH] &= ~_BV(y%8); 
}



// the most basic function, set a single pixel
void setPixel(unsigned char x, unsigned char y, unsigned char color) {
  if ((x >= LCDWIDTH) || (y >= LCDHEIGHT))
    return;

  // x is which column
  if (color) 
    pcd8544_buffer[x+ (y/8)*LCDWIDTH] |= _BV(y%8);  
  else
    pcd8544_buffer[x+ (y/8)*LCDWIDTH] &= ~_BV(y%8); 

  updateBoundingBox(x,y,x,y);
}


// the most basic function, get a single pixel
unsigned char getPixel(unsigned char x, unsigned char y) {
  if ((x >= LCDWIDTH) || (y >= LCDHEIGHT))
    return 0;

  return (pcd8544_buffer[x+ (y/8)*LCDWIDTH] >> (7-(y%8))) & 0x1;  
}


void begin(unsigned char contrast) {
  init();
  //  st7565_command(CMD_DISPLAY_ON);
  //st7565_command(CMD_SET_ALLPTS_NORMAL);
  //st7565_set_brightness(contrast);
}

void init(void) {
  // set pin directions
  /* TODO: Set up Zilog SPI
  pinMode(_din, OUTPUT);
  pinMode(_sclk, OUTPUT);
  pinMode(_dc, OUTPUT);
  pinMode(_rst, OUTPUT);
  pinMode(_cs, OUTPUT);
  */

  // toggle RST low to reset; CS low so it'll listen to us
  if (pcd8544_get_cs())
  {
	pcd8544_write_cs(0);
  }

  pcd8544_write_reset(0);
  delay(500);
  pcd8544_write_reset(1);


  // get into the EXTENDED mode!
  command(PCD8544_FUNCTIONSET | PCD8544_EXTENDEDINSTRUCTION );

  // LCD bias select (4 is optimal?)
  command(PCD8544_SETBIAS | 0x4);

  // set VOP
  command( PCD8544_SETVOP | 50); // Experimentally determined


  // normal mode
  command(PCD8544_FUNCTIONSET);

  // turn all the pixels on
  command(PCD8544_DISPLAYCONTROL | PCD8544_DISPLAYALLON);


  // initial display line
  // set page address
  // set column address
  // write display data

  // set up a bounding box for screen updates

  updateBoundingBox(0, 0, LCDWIDTH-1, LCDHEIGHT-1);
}

void command(unsigned char c) {
	_drop_dc();
  spiwrite(c);
}

void data(unsigned char c) {
	_raise_dc();
  spiwrite(c);
}

void setContrast(unsigned char val) {
}


void display(void) {
  unsigned char col, maxcol, p;
  
  for(p = 0; p < 6; p++) {
#ifdef enablePartialUpdate
    // check if this page is part of update
    if ( yUpdateMin >= ((p+1)*8) ) {
      continue;   // nope, skip it!
    }
    if (yUpdateMax < p*8) {
      break;
    }
#endif

    command(PCD8544_SETYADDR | p);


#ifdef enablePartialUpdate
    col = xUpdateMin;
    maxcol = xUpdateMax;
#else
    // start at the beginning of the row
    col = 0;
    maxcol = LCDWIDTH-1;
#endif

    command(PCD8544_SETXADDR | col);

    for(; col <= maxcol; col++) {
      //uart_putw_dec(col);
      //uart_putchar(' ');
      data(pcd8544_buffer[(LCDWIDTH*p)+col]);
    }
  }

#ifdef enablePartialUpdate
  xUpdateMin = LCDWIDTH - 1;
  xUpdateMax = 0;
  yUpdateMin = LCDHEIGHT-1;
  yUpdateMax = 0;
#endif

}

// clear everything
void clear(void) {
  memset(pcd8544_buffer, 0, LCDWIDTH*LCDHEIGHT/8);
  updateBoundingBox(0, 0, LCDWIDTH-1, LCDHEIGHT-1);
}

/*
// this doesnt touch the buffer, just clears the display RAM - might be handy
void clearDisplay(void) {
  
  unsigned char p, c;
  
  for(p = 0; p < 8; p++) {

    st7565_command(CMD_SET_PAGE | p);
    for(c = 0; c < 129; c++) {
      //uart_putw_dec(c);
      //uart_putchar(' ');
      st7565_command(CMD_SET_COLUMN_LOWER | (c & 0xf));
      st7565_command(CMD_SET_COLUMN_UPPER | ((c >> 4) & 0xf));
      st7565_data(0x0);
    }     
    }

}

*/
